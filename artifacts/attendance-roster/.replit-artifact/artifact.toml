kind = "web"
previewPath = "/"
title = "Attendance Roster"
version = "1.0.0"
id = "artifacts/attendance-roster"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24408

[services.development]
run = "pnpm --filter @workspace/attendance-roster run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/attendance-roster", "run", "build" ]
publicDir = "artifacts/attendance-roster/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24408"
BASE_PATH = "/"
